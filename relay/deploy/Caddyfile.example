# Replace pair.example.com with the DNS name that points to the Oracle VPS.
pair.example.com {
    encode zstd gzip
    reverse_proxy 127.0.0.1:8080

    header {
        Strict-Transport-Security "max-age=31536000; includeSubDomains"
    }
}
