{
  "name": "blazzing-pairing-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "wrangler deploy --dry-run --outdir .wrangler-dist",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "smoke": "bash test-smoke.sh"
  },
  "devDependencies": {
    "wrangler": "4.135.0"
  }
}
