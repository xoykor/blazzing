The Windows release workflow downloads the current x64 mpv build into this directory before packaging.

For local development, either:
- place mpv.exe here, or
- install mpv.exe in PATH, or
- set VIPTV_MPV_PATH to the full executable path.

The GitHub release package is expected to include mpv under resources/mpv.
